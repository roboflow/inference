torch>=2.0.1,<3.0.0
torchvision>=0.15.0
transformers>=4.57.3,<5.16.0
# diffusers added alomng with Cosmos 3, but the moment we wanted to
# include this model to the default build difusers 0.40.0dev1 (with
# patch required for Cosmos3OmniPipeline to work (part of inference-models
# `Cosmos3EdgeWorldModel` class which we don't yet registered weights on the platform)
# anyone using it locally should install
# diffusers @ git+https://github.com/huggingface/diffusers.git@86e6dac5360703ddf09fe250db50be667eb93662
diffusers>=0.39.0,<0.41.0
timm>=1.0.0
accelerate>=1.0.0,<2.0.0
einops>=0.7.0
peft>=0.15.0
num2words~=0.5.14
bitsandbytes>=0.42.0
pyvips[binary]>=2.2.3
