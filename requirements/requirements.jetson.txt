pypdfium2>=4.11.0,<5.0.0
jupyterlab>=4.3.0,<5.0.0
PyYAML~=6.0.0
inference-models~=0.28.1  # keep in sync between requirements.jetson requirements.gpu.txt, requirements.cpu.txt, requirements.vino.txt
sam3==0.1.3  # GPU-only core model; no longer pulled via inference-models base deps (see requirements.sam3.txt)
triton<4.0.0  # GPU-only; no longer pulled via inference-models base deps
