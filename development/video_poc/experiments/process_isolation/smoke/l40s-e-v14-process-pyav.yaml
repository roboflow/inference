apiVersion: v1
kind: Pod
metadata:
  name: video-process-smoke-e-4f1767d4
  namespace: video-proc
  labels:
    app: video-processor-process-smoke
    roboflow.com/environment: staging
    roboflow.com/variant: e-v14-process-pyav
spec:
  automountServiceAccountToken: false
  activeDeadlineSeconds: 180
  terminationGracePeriodSeconds: 10
  restartPolicy: Never
  imagePullSecrets:
    - name: gcp-ar-pull-secret
  nodeSelector:
    gpu_type: L40S
  tolerations:
    - key: gpu
      operator: Equal
      value: "true"
      effect: NoSchedule
  containers:
    - name: smoke
      image: us-central1-docker.pkg.dev/roboflow-staging/video-proc/video-processor-process@sha256:4f1767d45ec3d90e07215f377ebbbba21b7c8b1a42ffa8acedf4b6217c06a70c
      command: ["python", "-c"]
      args:
        - >-
          import importlib, multiprocessing as mp, os, torch;
          import processor;
          from job_process import resolve_job_execution_mode;
          from video_ingest import resolve_video_ingest_mode;
          ctx = mp.get_context("spawn");
          child = ctx.Process(target=importlib.import_module, args=("processor",), name="e-process-smoke-child");
          child.daemon = True;
          child.start();
          child.join(60);
          print("variant=e");
          print("execution_mode=" + resolve_job_execution_mode());
          print("ingest_mode=" + resolve_video_ingest_mode());
          print("start_method=" + ctx.get_start_method());
          print("supervisor_pid=" + str(os.getpid()));
          print("child_pid=" + str(child.pid));
          print("child_exitcode=" + str(child.exitcode));
          print("cuda_available=" + str(torch.cuda.is_available()));
          print("cuda_device=" + (torch.cuda.get_device_name(0) if torch.cuda.is_available() else "none"));
          assert resolve_job_execution_mode() == "process";
          assert resolve_video_ingest_mode() == "pyav";
          assert ctx.get_start_method() == "spawn";
          assert child.pid != os.getpid();
          assert child.exitcode == 0;
          assert torch.cuda.is_available();
          assert "L40S" in torch.cuda.get_device_name(0)
      env:
        - name: PROJECT
          value: roboflow-staging
        - name: PROCESSOR_JOB_EXECUTION_MODE
          value: process
        - name: PROCESSOR_EXECUTION_DOMAIN_MODE
          value: in_process
        - name: PROCESSOR_VIDEO_INGEST_MODE
          value: pyav
        - name: ENABLE_TENSOR_DATA_REPRESENTATION
          value: "true"
        - name: USE_INFERENCE_MODELS
          value: "true"
        - name: WORKFLOWS_IMAGE_TENSOR_DEVICE
          value: cuda
      resources:
        requests:
          cpu: "1"
          memory: 2Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "2"
          memory: 4Gi
          nvidia.com/gpu: "1"
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
        seccompProfile:
          type: RuntimeDefault
