# Staging experiment ledger

This file is the reconciliation point between live staging experiments and
deployable Git state. Production changes are out of scope.

For every staging mutation, add a row before applying it. Update the same row
with measurements, disposition, and a Git/image reference before moving to the
next experiment. Do not leave useful live configuration represented only in a
`kubectl` command or shell history.

| ID | UTC window | Hypothesis | Live target and change | Git/image source | Revert | Result artifact | Disposition |
|---|---|---|---|---|---|---|---|
| `nvdec-runtime-gate-001` | `2026-08-13T16:09:23Z`–`16:12:25Z` | The immutable v1.4 experiment image contains a working CUDA/GStreamer tensor runtime on the staging L40S before it joins the ready pool | Disposable `ck8s-stg/video-proc` pod `nvdec-capability-probe-75c1cc0c` requested one GPU, used `gcp-ar-pull-secret`, ran the CUDA discoverability probe on `np-6d1c92ba-58.us-east1-a.compute.internal`, exited zero, and was deleted after log collection | Cloud Build `c50c574e-09fc-47f0-9a99-5a8278b0f826`; source `8ecb821a7a8ef06516c162cc7e657ebc74947c61`; exact image `video-processor-nvdec@sha256:75c1cc0cd410e390f9b6db356e597e4c65c45282573b753d66bc91a6fe9a0bf8` | none; the disposable pod was deleted | pod log: `gstreamerCuda.available=true`, reason `ok`, `torchCudaAvailable=true`, device `NVIDIA L40S`; exact image pull was 9,751,192,551 bytes | pass; runtime gate cleared for a controlled ready-pool baseline |
| `gpu-pyav-tensor-001` | `2026-08-13T16:15Z`–`17:03Z` | PyAV CPU decode plus the v1.4 tensor workflow and demand-driven backpressure provides the decoder-only control for the NVDEC A/B | Only `ck8s-stg/video-proc` GPU ready-pool Deployment UID `f001fcc0-4cc7-44a5-9b5f-c6f1010a1e4d` changed. Revision `10` exposed a fixed two-second replay/RTSP-open race, so source `6ca38194b` added bounded transient-open retries and actual stream metadata; generation `12`/revision `11`/template SHA-256 `da36d40c2b8d36da8aa188e1471c8a962791e8922df47e5abd1de91b14989fef` keeps one replica in PyAV+tensor mode. The two exact cancelled-job pods were deleted after the worker failed to observe API cancellation promptly; CPU pool, relay, and production were unchanged | Cloud Build `f42cb459-b1e8-4e11-84ba-8ce2b2840835`; source `6ca38194bdc3c312c0adf6a3a275b9014c79f4b6`; exact image `video-processor-nvdec@sha256:214196ff30e8ac912830617138d32789c08456349528e0dd44e42cba7e8ac326`; staging infra commit `522da3d7` records repository, tag, digest, PyAV, tensor, and adaptive-backpressure settings | `kubectl --context ck8s-stg -n video-proc rollout undo deployment/video-processor-pool --to-revision=10`, wait for rollout, verify digest `75c1cc0c...`; also revert staging-only infra commit `522da3d7` before any apply | ignored reports `gpu-pyav-tensor-c1-r1-retry1-20260813` and `r2-retry1`; both operationally succeeded with no recovery. Actual RTSP stream: H.264, 1280x720, 60/1 FPS. After 10-second warmup, delivered FPS `2.543/2.512` (`50.9%/50.2%` of requested 5), frame latency p95 bucket `500/500 ms`, mean `317.5/324.9 ms`, startup `11.74/11.69 s`, counter deltas `425/420` inferred, `312/317` dropped, and zero image-output host materializations | fail; baseline is reproducible but misses both target-FPS and 50 ms latency gates. Keep the staging-only fixed image for diagnosis; do not advance to NVDEC or broader concurrency |
| `gpu-source-fps-limiter-001` | `2026-08-13T17:29Z`–`18:04Z` | Moving `maxFps` from the legacy post-collection wall-clock gate to VideoSource's declared-FPS stride avoids cadence aliasing; deferring PyAV BGR conversion until `retrieve()` also keeps skipped 60 FPS frames from consuming host conversion work | Operator `crusoe-client-admin-ck8s-stg` changed only `ck8s-stg/video-proc` GPU ready-pool Deployment UID `f001fcc0-4cc7-44a5-9b5f-c6f1010a1e4d`: added `ENABLE_FRAME_DROP_ON_VIDEO_FILE_RATE_LIMITING=True`, generation `13`/revision `12`, ReplicaSet `video-processor-pool-5fd8c859d9`. The exact image stayed unchanged. Two pods whose cancelled jobs remained active were recycled; Deployment replacements restored the ready pool. CPU pool generation `7` and relay generation `3` remained ready and untouched; production was not read or changed | live flag-only leg used source `6ca38194bdc3c312c0adf6a3a275b9014c79f4b6` and exact image `video-processor-nvdec@sha256:214196ff30e8ac912830617138d32789c08456349528e0dd44e42cba7e8ac326`; combined source `244ec9728` contains the not-yet-deployed deferred-materialisation and low-latency uploaded-replay fixes; source `53ec804cc` corrects burst-sensitive `stats.fps` | `kubectl --context ck8s-stg -n video-proc set env deployment/video-processor-pool ENABLE_FRAME_DROP_ON_VIDEO_FILE_RATE_LIMITING-`, wait for rollout, and verify the exact image digest is unchanged | ignored uploaded-file controls `gpu-source-fps-flag-upload-r1-retry1-20260813` and `upload-r2-20260813`: exact H.264 1280x720@60 source; after 10-second warmup delivered `4.913/4.840 FPS` (`98.3%/96.8%` of target), inferred deltas `821/809` over `167.124/167.155 s`, zero counted drops, p95 `20/20 ms`, mean `19.3/18.0 ms`. External-validity run `gpu-source-fps-flag-connector-traffic-r1-20260813` used the same connector-published file through MediaMTX; its actual stream also remained H.264 1280x720@60 and delivered `4.999 FPS`, 836 inferred over 167.241 s, zero counted drops, mean `26.5 ms`, p95 `35 ms`. The legacy EMA falsely ended uploaded `stats.fps` at `27.62/29.12`; measured counter/time throughput is authoritative and source `53ec804cc` fixes that field | pass; both deterministic repetitions exceed the `4.5 FPS` gate and the real connector source corroborates it, so the maxFps blocker is cleared for the controlled NVDEC A/B. Keep file controls authoritative because connector availability is external. The flag placement alone cleared the gate; neither candidate image fix was live in these results |
| `gpu-pack-current-001` | `2026-08-12T21:35Z`–pending | The current L40S worker has useful capacity above the existing four-job policy cap | `ck8s-stg/video-proc` Deployment UID `f001fcc0-4cc7-44a5-9b5f-c6f1010a1e4d`: GPU `MAX_CONCURRENT_JOBS` changed `4` to `24`; prior generation `8`, revision `7`, pod-template SHA-256 `b3eecf9e29751dcdd1c3717734824abe9e06b198aac273bed6355a6d07f88545`; staging infra PR #2454 was applied at `2026-08-13T00:25Z`, reconciling the live setting and rolling the pinned telemetry image with 2 GiB shared memory; Prometheus reports capacity `24` with `processor_tier=gpu` | exact worker image `video-processor-telemetry@sha256:50d4c922f5cd760f43fd982e04819c9a9ad18a1e17a43f67268ff8f917c80e6a`; source `c63f9720c25a27e7aa290cea601b09590a6de9f2`; harness `4db351b2a`; applied staging infra PR #2454 | revert staging PR #2454 values to the prior worker image and `MAX_CONCURRENT_JOBS=4`, apply staging only, wait for rollout, and verify the worker reports capacity 4 | ignored reports c5/c6/c8; c5 `94.411 FPS`, `13.4%` spread, `37.9 ms`, GPU `7.89%` avg; c6 `75.868 FPS`, `10.6%`, `88.4 ms`, GPU `7.05%`; c8 `55.464 FPS`, `41.5%`, `104.3 ms`, GPU `4.63%` | active for controlled-FPS tests; direct cluster drift is reconciled in applied staging configuration; strict unbounded max passing concurrency is 4; cap is not certified capacity |
| `gpu-controlled-fps-001` | `2026-08-13T01:01:49Z`–`01:38:35Z` | A 5 FPS cap exposes the useful packed-stream knee without asking every stream to run flat out | No persistent change; staging service API placed two repetitions each of c1/c4/c8/c12/c18 `single-detection` jobs on one L40S worker, output disabled and requested `maxFps=5` | exact worker image `video-processor-telemetry@sha256:50d4c922f5cd760f43fd982e04819c9a9ad18a1e17a43f67268ff8f917c80e6a`; runtime source `c63f9720c25a27e7aa290cea601b09590a6de9f2`; harness/analyzer `fba35775f`; applied staging infra PR #2454 | runner cancelled every created job after its window; no configuration revert required | ignored suite `gpu-controlled-fps-20260812b`; aggregate delivered FPS across repetitions: c1 `2.457/2.462` at `20 ms`, c4 `9.706/9.838` at `20 ms`, c8 `19.694/19.824` at `35 ms`, c12 `31.632/32.094` at `150 ms`, c18 `29.412/22.534` at `250/500 ms`; fairness spread remained `<=5.1%` in every successful run; the second c18 run lost one pipeline after its frame counter stalled for about 35 seconds | no 5-FPS capacity is certified because even c1 delivered only about `49%` of its requested target; latency knee is between c8 and c12 and c18 is unstable; investigate the max-FPS under-delivery separately, then use c1/c4/c8 and conditional c12 as the legacy A/B for the new model manager |
| `cpu-pack-current-001` | `2026-08-12T22:03:29Z`–`22:12:34Z` | The current CPU worker can pack several copies of the exact detector used in the GPU baseline without violating throughput, fairness, or latency gates | No persistent change; staging service API placed c1/c2/c3/c4 homogeneous `single-detection-cpu` runs on one CPU worker, staging canonical YOLOv8 Nano 640 model ID `microsoft-coco-obj-det/8`, output disabled, 90-second measured windows | worker image `video-processor-cpu:2e4a97ee5`; corpus profile `b42873a93`; analyzer `c111d5b39` | runner cancelled all jobs after each window; no configuration revert required | ignored reports `cpu-single-detection-c{1,2,3,4}-20260812a`; c1 `9.417 FPS`, `39.12 ms`; c2 `18.570 FPS`, `2.9%` spread, `43.38 ms`; c3 `27.403 FPS`, `3.5%`, `55.72 ms`; c4 `30.842 FPS`, `37.8%`, `190.58 ms`; CPU averaged `1.287`, `2.360`, and `4.953` cores at c1/c2/c4 and hit the 8-core limit at c4 | strict SLO passes through c2; c3 is throughput/fairness-safe at about 9 FPS per stream but narrowly misses the 50 ms latency gate; c4 is a real CPU saturation knee; this is a model-for-model YOLOv8 Nano CPU/GPU comparison |
| `l40s-mps-capability-001` | `2026-08-12T23:45:42Z`–`2026-08-13T00:14:41Z` | The current staging L40S runtime can support an exclusive-pod raw-MPS experiment, while MIG and device-plugin MPS remain unavailable | Read-only probe of ready GPU pod `video-processor-pool-9946c99c5-g2f55` on `ck8s-stg/video-proc`, node `np-6d1c92ba-58.us-east1-a.compute.internal` (`l40s-48gb.10x`); no process or cluster mutation | exact current worker image `video-processor@sha256:55833b57c6f46484a7c1ccee305f3ef0e4cf83d08cada8b84df41f346cc274b3`; MMP experiment PR #2788 revision `5f02db12ebdda013ff92e6607f92f88c7f9582ec`; Cloud Build `f0bd00b6-f0d1-410a-8ba6-bf9f8e8ccfd0` produced `mmp-benchmark@sha256:6a6592f77e0eb1d3bfc8b82d7add6a7206e946a437a072f7f3a58cf693b1716d`; infra PR #2454 provides memory-backed `/dev/shm` and private MPS directories | none; read-only probe and image build only | driver `570.133.20`; Torch `2.6.0+cu124`; CUDA `12.4`; one visible L40S, `46068 MiB`, compute mode `Default`; `nvidia-cuda-mps-control` and `nvidia-cuda-mps-server` present; current `/dev/shm` only `64 MiB`; MIG mode `N/A`; node labels `nvidia.com/mig.capable=false`, `nvidia.com/mps.capable=false`, `nvidia.com/gpu.sharing-strategy=none`, replicas `1` | image ready; raw MPS inside one exclusive-GPU benchmark pod is viable to test after the dedicated 4 GiB shared-memory Deployment is approved; Kubernetes device-plugin MPS sharing and MIG are out for this L40S fleet |
| _example_ | _2026-08-12T00:00Z_ | _Higher worker capacity exposes the GPU knee_ | _staging `video-proc` GPU pool: `MAX_CONCURRENT_JOBS=4` to `24`_ | _commit SHA; immutable image digest; rendered values SHA-256_ | _restore prior Deployment revision and verify ready replicas_ | _ignored `results/SUITE_ID/suite.json`; durable summary committed separately_ | _pending / keep / revert_ |

## Required evidence

- Record the cluster context, namespace, workload UID, prior revision, image
  digest, rendered configuration hash, and operator identity.
- Record the exact benchmark suite/run IDs and the Prometheus time window.
- Keep API keys, access tokens, source URLs containing credentials, and generated
  job files out of the ledger and Git.
- A successful experiment is not complete until its reusable code/config is
  committed and pushed. A rejected experiment is not complete until staging is
  reverted and the reason is recorded.
- Before a production proposal, verify the live staging workload can be recreated
  from the recorded commits and immutable images without manual patches.
