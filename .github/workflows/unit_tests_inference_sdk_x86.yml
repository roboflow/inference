name: Unit tests for core components of inference SDK package

on:
  pull_request:
    branches: [main]

jobs:
  build-dev-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11", "3.12"]
    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - name: 🐍 Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          check-latest: true
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install setuptools
          pip install -r requirements/requirements.cli.txt -r requirements/requirements.test.unit.txt
      - name: 🧪 Unit Tests of Inference
        timeout-minutes: 30
        run: python -m pytest tests/inference_sdk/unit_tests
