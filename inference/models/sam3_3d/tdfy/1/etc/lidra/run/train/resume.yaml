# @package _global_

hydra:
  job:
    chdir: false
  run:
    dir: /tmp/lidra/timestamped/${now:%Y-%m-%d}/${now:%H-%M-%S}_${tag.name}
  sweep:
    dir: /tmp/lidra/timestamped/${now:%Y-%m-%d}/${now:%H-%M-%S}_${tag.name}
  output_subdir: null
logs:
  path: ???
