# @package _global_
defaults:
  - fm-trellis-features-trellis500k-tricks
  
  # dataset
  - override /data/dataset/tdfy/trellis500k@loop.dataloaders.training.dataset: training_fo_v1_features

loop:
  max_epochs: 80
  callbacks:
    model_checkpoint2:
      every_n_epochs: 10