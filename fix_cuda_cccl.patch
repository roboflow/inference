--- a/docker/dockerfiles/Dockerfile.onnx.gpu
+++ b/docker/dockerfiles/Dockerfile.onnx.gpu
@@ -57,6 +57,15 @@
     pip install -U pip setuptools wheel uv && \
     uv pip install --system --break-system-packages numpy==1.26.4 pybind11
 
+# Fix CUDA 13.0 missing CCCL headers - fetch from NVIDIA repository
+RUN git clone --depth=1 --branch v2.6.1 https://github.com/NVIDIA/cccl.git /tmp/cccl && \
+    mkdir -p /usr/local/cuda/include/cuda && \
+    cp -r /tmp/cccl/libcudacxx/include/cuda/* /usr/local/cuda/include/cuda/ && \
+    mkdir -p /usr/local/cuda/include/cccl && \
+    cp -r /tmp/cccl/libcudacxx/include/cuda /usr/local/cuda/include/cccl/ && \
+    cp -r /tmp/cccl/cub /usr/local/cuda/include/cccl/ && \
+    cp -r /tmp/cccl/thrust /usr/local/cuda/include/cccl/ && \
+    rm -rf /tmp/cccl
+
 # Build ONNX Runtime from source for CUDA 13.0 (using main branch for latest CUDA 13 fixes)
 WORKDIR /tmp
